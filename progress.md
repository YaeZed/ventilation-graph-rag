# Progress: User Module P1 Backend Image Attachments

## 2026-05-27

- Started P1 implementation after user requested next phase.
- Created planning files per project instruction because this phase spans backend, frontend, UI, docs, and validation.
- Inspected current backend user module and frontend chat store/rendering. Confirmed P1 can be implemented by adding attachment records and preserving `imageUrl` as display URL while stripping data URLs for authenticated storage.
- Added `ConversationAttachment`, migration `users.0002`, upload/list/delete APIs, and development media routing.
- Updated frontend user API, chat store, PDF export, message image rendering, and image alignment styles.
- Updated `docs/api.md`, `docs/architecture.md`, `docs/runbook.md`, `docs/status.md`, and `docs/plan-user-module.md` to mark P1 implemented.
- Validation passed: Django check, migration check, attachment upload/list/delete smoke, `vue-tsc --build`, and `vite build`.
- Fixed assistant Markdown rendering for model-generated `<br>` line breaks inside tables by adding a shared safe Markdown renderer. Validation passed: `vue-tsc --build` and `vite build`.
- Renamed the product brand to `矿风眼` across the sidebar, home header, auth pages, and browser title. Added a reusable SVG `BrandMark` with mine-tunnel, eye, and wind-flow motifs. Validation passed: `vue-tsc --build` and `vite build`.
- Added fade transition for in-chat conversation switching by reusing the existing `.fade` transition around the active conversation pane. Validation passed: `vue-tsc --build` and `vite build`.
- Added per-conversation in-memory scroll position restore for the chat message window. Switching conversations restores the previous scroll position; new messages in the same conversation still scroll to bottom. Validation passed: `vue-tsc --build` and `vite build`.
- Reworked the chat bottom input bar from absolute overlay to the normal `home-view` grid footer, so the message window reserves real layout space instead of being covered by the input bar. Validation passed: `vue-tsc --build` and `vite build`.
- Started P2 stats panel enhancement. Updated `task_plan.md` from P1 attachment scope to P2 stats scope after confirming `docs/plan-user-module.md` defines P2 as `/stats` local statistics enhancement.
- Completed P2 stats panel enhancement: extended `ChatStats`, redesigned `/stats` with completion overview, risk distribution, trend bars, and scene distribution, and updated docs. Validation passed: `vue-tsc --build` and `vite build`.
- Ran neat-freak doc reconciliation after P2. Updated `AGENTS.md`, `README.md`, `docs/plan-user-module.md`, `docs/runbook.md`, `docs/architecture.md`, and planning files so P1/P2 status, attachment persistence, and `/stats` data ownership match the current code. No build was run because this sync only changed documentation.
- Started and completed P3 backend stats aggregation. Added authenticated `stats/summary`, `stats/trends`, and `stats/hazards` endpoints; wired logged-in `/stats` to refresh backend stats with local fallback; updated docs and planning files. Validation passed: `web_backend/manage.py check`, backend stats smoke test, `vue-tsc --build`, and `vite build`.
- Ran neat-freak doc reconciliation after P3. Updated `AGENTS.md` and planning notes so `/stats` is documented as guest-local plus logged-in backend aggregation, with team-level permissions/statistics remaining as the next unfinished layer. No build was run because this sync only changed documentation.
- Started P4 team permissions/statistics. Replaced the active plan with a P4 scope: team models, membership APIs, optional conversation team assignment, team-scoped backend stats, frontend team controls, docs, and validation.
- Completed P4 inspection. Backend needs `Team`, `TeamMembership`, nullable `ConversationRecord.team`, membership-gated team APIs, and `teamId` support on stats/sync. Frontend can keep one store and add team management to `/settings` plus a team selector on `/stats`.
- Implemented P4 backend foundation: `Team`, `TeamMembership`, nullable `ConversationRecord.team`, team CRUD/member routes, explicit conversation team assignment route, and `teamId` support on stats summary/trends/hazards. `web_backend/manage.py check` passed.
- Implemented P4 frontend wiring: team API client, Pinia team state/actions, settings-page team/member management, current-conversation team assignment, and `/stats` personal/team selector. Fixed one Vue type issue and confirmed `vue-tsc --build` once after the initial UI wiring.
- Applied `users.0003` locally and passed a backend P4 smoke test covering team creation, member addition, team conversation sync, team stats for a member, and forbidden stats access for a non-member.
- Updated P4 docs in `AGENTS.md`, `README.md`, `docs/api.md`, `docs/architecture.md`, `docs/runbook.md`, `docs/status.md`, and `docs/plan-user-module.md`.
- Final P4 validation passed: `web_backend/manage.py check`, `makemigrations --check --dry-run`, `vue-tsc --build`, and `vite build`.
- Started P5 production account security. Replaced the active plan with P5 scope after confirming P4 was complete. Initial inspection found CSRF exemptions on mutating account APIs, weak registration password checks, no login retry limit, and no persisted security audit events.
- Implemented P5 backend security baseline: CSRF bootstrap endpoint, removal of user-module CSRF exemptions, Django password validation, cache-backed login/register throttles, `SecurityEvent` model/API, session/cookie settings, and migration `users.0004_securityevent`.
- Implemented P5 frontend security wiring: centralized CSRF-aware `apiFetch()`, token refresh after login/register rotation, password placeholder update, Pinia security event state, and `/settings` account security panel.
- Applied `users.0004` locally and passed a P5 security smoke test covering CSRF 403, weak password 400, registration/security event reads, owner removal 403, non-member team stats 403, and login throttling 429.
- Final P5 validation passed: `web_backend/manage.py check`, `makemigrations --check --dry-run`, `vue-tsc --build`, and `vite build`.
- Started P4+ team conversation browser based on user feedback: team members need to open team-owned conversations, assignment should move to conversation menus, and `/settings` should remove active-conversation ownership controls.
- Implemented P4+ team conversation browser: added membership-gated team conversation API, frontend team conversation state, menu-based team assignment, sidebar "团队对话" section, and removed settings-page active conversation ownership control.
- Validation passed for P4+: backend check, migration dry-run, team conversation smoke test, `vue-tsc --build`, and `vite build`.
- Fixed the conversation "归属团队" submenu hover behavior so it stays open while moving from the main menu to the submenu, and moved the submenu to the right side of the conversation action menu. Validation passed: `vue-tsc --build` and `vite build`.
- Fixed the first-open settings team panel state by loading selected-team members immediately and after team-list refreshes where the selected team id does not change. Validation passed: `vue-tsc --build` and `vite build`.
- Fixed the team assignment submenu being clipped by the sidebar scroll container by teleporting it to the document body and positioning it beside the menu trigger with fixed coordinates. Validation passed: `vue-tsc --build` and `vite build`.
- Reworked the conversation team-assignment menu to use popover-style outside-click closing instead of parent-row mouseleave closing. The teleported submenu is now treated as part of the same open layer and is positioned against the main menu's right edge. Validation passed: `vue-tsc --build` and `vite build`.
- Updated team conversation sidebar metadata to include both the team name and the owner identity that placed the conversation into the team. Validation passed: `vue-tsc --build` and `vite build`.
- Updated settings UI: account security events now show a five-row scrollable list, and team managers can inline-edit the selected team name with a compact pencil control aligned to the existing select/input styling. Validation passed: `vue-tsc --build` and `vite build`.
- Fixed a settings-page runtime crash caused by an immediate watcher calling `cancelTeamNameEdit` before the const function initializer ran; converted the team-name edit handlers to hoisted function declarations. Validation passed: `vue-tsc --build` and `vite build`.
- Replaced native team-management selects with a lightweight styled `SettingsSelect` component to avoid system-blue option dropdowns, and refreshed team/members from the backend after team-space mutations. Validation passed: `vue-tsc --build` and `vite build`.
- Reused `SettingsSelect` for the stats scope dropdown so the `/stats` team/personal selector matches the team-management dropdown styling. Validation passed: `vue-tsc --build` and `vite build`.
- Fixed `SettingsSelect` text becoming white in page headers by overriding the global `.page-header button` color for select triggers/options and separating disabled selected-option styling. Validation passed: `vue-tsc --build` and `vite build`.
- Narrowed the global page-header button selector to direct action buttons so reused `SettingsSelect` instances keep their own trigger/option text and background styles without page-specific overrides. Validation passed: `vue-tsc --build` and `vite build`.
- Ran neat-freak reconciliation after P4+/P5 UI polish. Updated `AGENTS.md`, `docs/architecture.md`, `docs/runbook.md`, `docs/status.md`, `docs/plan-user-module.md`, `findings.md`, and `task_plan.md` so team conversation browsing, menu placement, account security list behavior, team inline rename, and `SettingsSelect` reuse match the current code. No build was run because this pass only changed documentation.
- Started team-space remark/delete polish. Confirmed backend already stores and updates `Team.description`; scope is `/settings` UI: show selected-team remark after the name, edit name and remark together, and add inline delete confirmation.
- Completed team-space remark/delete polish in `/settings`: selected-team title now shows a small remark label, the pencil opens a two-field name/remark edit form with explicit save/cancel, and owner-only deletion requires an inline confirm/cancel step. Validation passed: `vue-tsc --build`, `vite build`, and `git diff --check` with only existing CRLF warnings.
- Tightened the `/settings` team edit row so the expanded name/remark form no longer crowds the owner-only "删除团队" button. Validation passed: `vite build` and `git diff --check` with only existing CRLF warnings.
- Started `plan-sensor-multiimage` implementation. Replaced the active task plan with sensor data + multi-image recognition scope, read the module design doc, inspected the frontend chat store/HomeView/API, Django chat SSE endpoint, RAG pipeline, vision extractor, and generation module.
- Logged environment issues: `rg.exe` is denied in this sandbox, and `session-catchup.py` hit a Windows GBK `UnicodeEncodeError`; continued with `git diff --stat`, `git grep`, and PowerShell inspection.
- Added frontend multimodal foundations: shared `SensorData`/`ChatMessageImage` types, `SensorInputPanel.vue`, `SensorDataBadge.vue`, `MultiImageBar.vue`, and CSS for draft context panels, sensor chips, and multi-image thumbnails.
- Updated frontend chat transport/store/HomeView to accept multi-image arrays and structured sensor data, preserve message `images[]`/`sensorData`, upload multiple authenticated attachments, render multi-image message grids, and include sensor tables in PDF export.
- Extended Django chat endpoints for `sensor_data` and repeated multipart `images`; legacy single `image` remains supported. A fake-pipeline smoke verified JSON sensor calls, multipart multi-image calls, and SSE multimodal events.
- Extended `VentilationVisionExtractor` with `MultiImageResult`, per-image observation, merged concept retrieval, and multi-image joint analysis. Extended `VentilationRAGPipeline` routing and `VentilationGeneration` multimodal prompts for sensor-only, single-image+sensor, multi-image, and multi-image+sensor flows.
- Validation passed for `plan-sensor-multiimage`: Python compile check, `web_backend/manage.py check`, Django chat multimodal fake-pipeline smoke, `node node_modules/vue-tsc/bin/vue-tsc.js --build`, and `node node_modules/vite/bin/vite.js build`.
- Updated `README.md`, `AGENTS.md`, `docs/api.md`, `docs/architecture.md`, `docs/runbook.md`, `docs/status.md`, `docs/plan-sensor-multiimage.md`, `findings.md`, and `task_plan.md` for the completed module.
- Started sensor input dropdown polish after user feedback. Replaced the native sensor-type `select` with shared `SettingsSelect.vue` and scoped the row delete-button CSS so the shared select buttons keep their normal shape and alignment.
- Validation passed for the dropdown polish: `node node_modules\vue-tsc\bin\vue-tsc.js --build`, `node node_modules\vite\bin\vite.js build`, and `git diff --check` with only existing CRLF warnings.
- Started fixing user-reported scenario 2. Located the backend SSE crash in `web_backend/chat/views.py`: sensor-only streams hit a stale `image_path_text` variable after the multi-image refactor normalized paths into `image_path_texts`.
- Completed scenario 2 fix. Validation passed: `py_compile web_backend/chat/views.py`, `web_backend/manage.py check`, Django test-client SSE smoke for sensor-only JSON stream, and `git diff --check` with only existing CRLF warnings.
- Fixed the clarified sent-message image display issue. Backend now merges `ConversationAttachment` rows into serialized messages by `messageClientId`, and frontend remote/local conversation merge preserves local message media when the remote snapshot lacks media fields.
- Validation passed for sent-message image display fix: `py_compile web_backend/users/views.py`, `web_backend/manage.py check`, `vue-tsc --build`, `vite build`, and a backend serialization smoke proving a user message receives `attachments`, `images`, and `imageUrl` from its attachment row.
- Fixed the retested chat-window image display issue. The frontend now writes local data-URL thumbnails into the user message before attachment upload/backend sync, then swaps in persisted attachment URLs when available. Message thumbnails are clickable and open the image in a new tab. Validation passed: `vue-tsc --build`, `vite build`, and `git diff --check` with only existing CRLF warnings.
- Replaced new-tab image viewing with an in-page centered preview overlay in `HomeView.vue`. Users can close it by clicking the backdrop, the close button, or pressing Escape. Validation passed: `vue-tsc --build`, `vite build`, and `git diff --check` with only existing CRLF warnings.
- Fixed refresh-time image loss. Browser local storage now keeps compressed message thumbnails for authenticated users, while backend sync still strips data URLs. Authenticated attachment upload retries with the compressed preview when the original file upload fails, improving refresh/cross-device persistence. Validation passed: `vue-tsc --build`, `vite build`, `web_backend/manage.py check`, and `git diff --check` with only existing CRLF warnings.
- Ran neat-freak reconciliation after the sensor/multi-image and sent-image fixes. Updated `AGENTS.md`, `README.md`, `docs/api.md`, `docs/architecture.md`, `docs/runbook.md`, `docs/status.md`, `docs/plan-sensor-multiimage.md`, `findings.md`, and `task_plan.md` so local compressed previews, remote data-URL stripping, backend attachment retry, `messageClientId` restoration, and in-page image preview match the current implementation. No frontend/backend build was rerun because this pass only changed documentation.
- Fixed image preview polish: long preview images now stay inside the modal frame, and multi-image messages can be browsed with left/right arrows or keyboard left/right while staying in the current-page preview. Validation passed: `vue-tsc --build`, `vite build`, and `git diff --check` with only LF/CRLF warnings.
- Ran neat-freak reconciliation after the preview polish. Updated README/runbook/status/module plan/planning notes so the documented image preview behavior includes frame-constrained long images and multi-image arrow/keyboard navigation. No build was rerun because this pass only changed documentation.
- Started `plan-model-config-deploy` implementation. Per project rule, updated `task_plan.md` before code changes and scoped this pass to the runtime model configuration loop: settings UI, guest/account persistence, chat request payloads, and request-scoped backend model overrides. Deployment scaffolding is left as a later phase until the runtime configuration is verifiable.
- Implemented model-config frontend foundations: shared presets/types, `settings.modelConfig` normalization, request payload propagation through text/upload/SSE APIs, and a `/settings` model service panel reusing `SettingsSelect` and existing settings panel layout.
- Implemented backend model-config handling: account settings normalization, chat JSON/multipart parsing, and request-scoped RAG pipeline model/client overrides for text generation, query routing, hybrid/graph retrieval LLM calls, and vision extraction.
- Validation passed so far: Python compile check, `web_backend/manage.py check`, `vue-tsc --build`, and `vite build`. First fake-pipeline smoke patched the wrong module path and accidentally attempted real Neo4j initialization; logged and rerunning with the actual `chat.views` module.
- Tightened request-scoped model override locking so no-config requests also enter the shared model-state lock. This prevents a default request from observing another request's temporary text/VL client while preserving global Neo4j/Milvus resources. Validation passed: Python compile check, `web_backend/manage.py check`, `vue-tsc --build`, `vite build`, `git diff --check` with only LF/CRLF warnings, and a pipeline lock/restore smoke.
- Added `/settings` model-service actions for connection testing and restoring defaults. Backend `POST /api/chat/model/test/` returns separate text/vision results and masks submitted API keys in errors. Validation passed: `py_compile web_backend/chat/views.py`, `web_backend/manage.py check`, `vue-tsc --build`, `vite build`, and a fake OpenAI endpoint smoke. Deployment remains paused.
- Fixed the settings-page CSRF/error-display regression reported from `http://127.0.0.1:5174`: local Django CSRF defaults now include Vite's 5174 fallback, CSRF failures return JSON, runbook documents the port behavior, and frontend user/chat API parsing turns non-JSON HTML backend errors into concise actionable messages instead of rendering the raw debug page.
- Fixed the reported visual-model connection test failure by raising the backend model-test probe from `max_tokens=8` to `MODEL_TEST_MAX_TOKENS=16`, matching DashScope Omni's minimum token range.
- Ran neat-freak reconciliation for runtime model config and deployment notes. Updated README, AGENTS, API, architecture, runbook, status, plan-model-config-deploy, findings, and task_plan so deployment explicitly requires BYOK or a controlled tenant-key policy and must not let ordinary users consume the maintainer `.env` model key.
- Started deployment choice/design pass instead of writing deployment code. Read `docs/plan-model-config-deploy.md`, current planning files, status/runbook/architecture, requirements, compose, Django settings, frontend package scripts, and model-config profile-sync code.
- Added `docs/deployment-decision-matrix.md` with a deployment comparison and recommended baseline: 4C8G VPS/ECS, nginx same-origin routing, Django/Gunicorn, Vue static files, Postgres, persistent media, external Neo4j/vector services where possible, and `BYOK local-only` for public deployment.
- Updated `task_plan.md` Phase 8 to complete as a decision/planning phase and added Phase 12 for implementation after the user chooses hosting/key/data-store policy. Updated `findings.md` with deployment constraints, including the current `settings.modelConfig` profile-sync risk that must be fixed for `BYOK local-only`.
- Analyzed deployment cost drivers using current official pricing pages. Added `docs/deployment-cost-analysis.md` and updated planning notes: fixed MVP cost can stay around 50-150 RMB/month with discounted 4C8G + free graph/vector tiers + BYOK, while maintainer-paid image model calls and paid Neo4j are the major cost jumps.
